import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.palastine.Blood',
  appName: 'Blood Bank ',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
